DATA:
  DATASET: SPECIFY_WITH_CMD
  EXTENSION: mp4
  VAL_MIN_STEPS: 2
  VAL_MAX_SEGS: 1
  STRIDE: 2

TRAIN:
  EPOCHS: 25
  EPOCHS_WARMUP: 3
  EPOCHS_SAVE: 1
  EPOCHS_EVAL: 1
  LR_SCHEDULER:
    EPOCHS_DECAY: 3

AUG:
  MIN_SCALE: 0.9
  TYPE_MIXUP: back

  PROB_SCALE: 1.0
  PROB_SHIFT: 0.3
  PROB_SHAKE: 0.3
  PROB_ZOOM: 0.3
  PROB_PERSPECTIVE: 0.8
  PROB_CLONE: 0.15
